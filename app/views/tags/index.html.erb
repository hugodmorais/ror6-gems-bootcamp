Tags

<% @tags.each do |tag| %>
  <div class="row">
    <%= tag.name %>
    <%= tag.course_tags_count %>
    <%= link_to 'Destroy', tag, method: :delete, data: { confirm: 'Are you sure?' } %>
  </div>
<% end %>